
  # Duplicate existing code

  This is a code bundle for Duplicate existing code. The original project is available at https://www.figma.com/design/PzumQeWFlKlYZJuyrnM6cx/Duplicate-existing-code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  